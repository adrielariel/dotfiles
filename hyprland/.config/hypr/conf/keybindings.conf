$mainMod = SUPER
$HYPRSCRIPTS = ~/.config/hypr/scripts

# Applications
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, e, exec, $fileManager
bind = $mainMod, space, exec, nc -U /run/user/1000/walker/walker.sock

# Windows
bind = $mainMod, q, killactive
bind = $mainMod, f, fullscreen, 0
bind = $mainMod, m, fullscreen, 1
bind = $mainMod, t, togglefloating
bind = $mainMod, g, togglegroup
bind = $mainMod, p, pin

# Walker modules
bind = $mainMod, v, exec, uwsm-app -- walker -m clipboard        # CRUCIAL, KEEP THAT EXTRA SPACE AT THE BACK
bind = $mainMod, a, exec, uwsm-app -- walker -m todo

# Actions
bind = $mainMod SHIFT, m, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod CTRL, r, exec, hyprctl reload
bind = $mainMod SHIFT, b, exec, hyprland-toggle-waybar
bind = $mainMod CTRL, l, exec, systemctl suspend
bind = $mainMod CTRL, w, exec, waypaper
# bind = $mainMod SHIFT, s, exec, $HYPRSCRIPTS/screenshot.sh

# Move focus
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Swap windows
bind = $mainMod ALT, h, swapwindow, l
bind = $mainMod ALT, l, swapwindow, r
bind = $mainMod ALT, k, swapwindow, u
bind = $mainMod ALT, j, swapwindow, d

# Resize windows
bindle = $mainMod SHIFT, right, resizeactive, 25 0
bindle = $mainMod SHIFT, left, resizeactive, -25 0
bindle = $mainMod SHIFT, down, resizeactive, 0 25
bindle = $mainMod SHIFT, up, resizeactive, 0 -25

# Move/resize windows with mouse
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, Tab, workspace, m+1
bind = $mainMod SHIFT, Tab, workspace, m-1

# Scratchpad
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Media keys
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 2%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -q s +1%
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -q s 1%-

bindl = ,XF86AudioNext, exec, playerctl next
bindl = ,XF86AudioPause, exec, playerctl play-pause
bindl = ,XF86AudioPlay, exec, playerctl play-pause
bindl = ,XF86AudioPrev, exec, playerctl previous
